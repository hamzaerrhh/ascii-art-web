# ASCII Banner Printer

This program prints stylized ASCII text using a banner file (`standard.txt`).

## Usage

1. Make sure `main.go` and `standard.txt` are in the same folder.
2. Run the program with a text argument.

Example:

```bash
go run main.go "HELLO"
